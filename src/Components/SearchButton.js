// components/SearchButton.js

import React from 'react';

const SearchButton = () => {
  const handleSearch = () => {
    // Handle the search functionality here
    // You can add the necessary code to perform the search
  };

  return (
    <button onClick={handleSearch}>
      Search
    </button>
  );
};

export default SearchButton;